{
  "name": "hello-mick",
  "description": "ClaudiaJS hello-world example using DockPress",
  "version": "1.0.0",
  "private": true,
  "files": [
    "main.js"
  ],
  "scripts": {
    "start": "claudia create --name hello-mick --region us-east-1 --handler main.handler",
    "test": "claudia test-lambda",
    "deploy": "claudia update"
  },
  "devDependencies": {
    "claudia": "^1.x"
  }
}
